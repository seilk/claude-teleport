{
  "name": "teleport",
  "version": "0.1.0",
  "description": "Beam your Claude Code setup across machines",
  "type": "module",
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test src/__tests__/**/*.test.ts",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
